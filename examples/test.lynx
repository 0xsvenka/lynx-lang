import std::math

f = | i, j => i + j

a = 2; b = a;
c0_' = '\0'

s = "bc\n\t\\\"";
multi_line_str =
  \\This is a multi-line
  \\string literal.
  \\    Add some indentation.
    \\Where the line starts does not matter.
  \\No escape sequence is processed here:
  \\" ' \ \\ \n
  \\You will get a trailing '\n' due to the next line
  \\

-- A comment
-- Another comment

rtol (1..100 by 3)
  |> map (| x => x + 1)
  |> filter math::is_prime
  |> reduce (+)
  |> println

bool_to_str =
  | true => "true"
  | false => "false"

ii = 3;
ss = ii |>
  | 1 | 2 => "small"
  | _ => "big"
