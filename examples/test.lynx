import std::math

ctor Option: Type -> Type

f = i => j => i + j;
g = (i; j) => i + j

c0_' = '\0';
-- Error!
bad_ch_1 = ''; bad_ch_2 = 'aba'; bad_ch_3 = 'a

s = "bc\n\t\\\";  -- Error!
multiline_str =
  "This is a multi-line" \\string literal.
  -- Comments do not break the multi-line string
  \\ \n \t
  ;
println(multiline_str)  -- A comment

-- A comment in the middle

list::from(range(1, 100, 3))
  |> map(x => x + 1) |> filter(math::is_prime)
  |> reduce((+))
  |> println

_ = case 3 of
  | 1 | 2 => "small"
  | _ => "big"
  end

a +-- 1321;  -- `--` in `+--` does NOT begin a comment
a-- 13d;
