import std::math

ctor Option : Type -> Type

f = | i j => i + j

c0_' = '\0';
-- Here I use `!` after `--` to comment out erroneous code
bad_ch_1 = ''; bad_ch_2 = 'aba'; bad_ch_3 = 'a

s = "bc\n\t\\\";
multiline_str =
  "This is a multi-line"
  \\string literal.
  -- Comments do not break the multi-line string
  \\ \n \t
  ;
println multiline_str  -- A comment

-- A comment in the middle

std::list::from (1..100 by 3)
  |> map (| x => x + 1) |> filter math::is_prime
  |> reduce (+)
  |> println

_ = 3 |>
  | 1 | 2 => "small"
  | _ => "big"

ad +-- 1321;  -- `--` in `+--` does NOT begin a comment
ad a-- 13d;
1a
