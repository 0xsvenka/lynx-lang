import std.math

println("Hello, World!")

f = i => j => i + j
incr = f(1, ~)

var a = 12345678
a := 2; b = a

c = "bc\n\t\\\"
...and next line"
# A comment
# Another comment
println(c)

type Point = { x = ?, y = ? }

(1..100).to_list() \
.map(i => i + 1) \
.filter(math.is_prime) \
.reduce((+)) \
$ println

fn len(l) =
    match l with
        [] -> 0,
        [_, ...tail] -> len(tail) + 1
assert_eq(len([1, 2, 3]), 3)
